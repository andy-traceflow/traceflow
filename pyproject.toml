[project]
name = "traceflow"
version = "0.1.0"
description = "SIA Kit — single-tenant Shopify → destination integration template"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }

dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
    "asyncpg>=0.30.0",
    "httpx>=0.28.0",
    "pyjwt[crypto]>=2.10.0",  # RS256 service-account assertion for the Google Sheets adapter
    "python-dotenv>=1.0.1",  # pydantic-settings env_file support
    "jsonpath-ng>=1.6.1",  # mapping.yaml `from:` expressions
    "pyyaml>=6.0.2",  # mapping.yaml
    "sentry-sdk[fastapi]>=2.18.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-mock>=3.14.0",
    "ruff>=0.7.0",
    "mypy>=1.13.0",
    "types-pyyaml>=6.0.12",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

# Render's Python buildpack runs `poetry install` as a pre-step regardless of
# the declared build-backend. Without this flag Poetry tries to install the
# root project itself and fails because our package layout is `src/app/`, not
# `traceflow/`. Setting package-mode=false makes Poetry act as a dependency
# installer only; the project is then installed by render.yaml's
# `buildCommand: pip install -e .` via setuptools.
[tool.poetry]
package-mode = false

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "ASYNC"]
ignore = ["E501"]

[tool.ruff.lint.flake8-bugbear]
# FastAPI's `Depends()` is the canonical way to declare dependencies in
# route handlers. Ruff's B008 rule normally warns about function calls in
# default arguments — whitelisting these treats them as immutable so the
# pattern doesn't trip the linter.
extend-immutable-calls = [
    "fastapi.Depends",
    "fastapi.Query",
    "fastapi.Header",
    "fastapi.Path",
    "fastapi.Cookie",
    "fastapi.Body",
    "fastapi.Form",
    "fastapi.File",
    "fastapi.Security",
]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unreachable = true

[[tool.mypy.overrides]]
module = "jsonpath_ng.*"
ignore_missing_imports = true
